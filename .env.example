# Production must use a same-origin server gateway. Never place CRM secrets here.
PUBLIC_LEAD_ADAPTER=gateway
PUBLIC_LEAD_ENDPOINT=/api/leads

# Set only after the current heyzeno.com redirect has been removed and verified.
ZENO_DOMAIN_REDIRECT_REMOVED=false
